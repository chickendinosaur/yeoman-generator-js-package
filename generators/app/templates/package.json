{
	"name": "",
	"version": "0.0.1",
	"main": "index.js",
	"files": [
		"**/*.js"
	],
	"scripts": {
		"benchmark": "npm run test && node src/index.benchmark.js",
		"build": "npm run clean && babel src -d build && babel build -d lib --ignore *.*.js && cp -f package.json lib",
		"clean": "rm -rf lib && rm -rf build*",
		"deploy": "npm run test && npm publish lib/",
		"test": "npm run build && tap build/**/*.spec.js --reporter spec"
	},
	"author": "",
	"license": "MIT",
	"description": "",
	"keywords": [],
	"private": false,
	"repository": {
		"type": "git",
		"url": "git+https://github.com/repoNamespace/repoName.git"
	},
	"bugs": {
		"url": "https://github.com/repoNamespace/repoName/issues"
	},
	"homepage": "https://github.com/repoNamespace/repoName#readme",
	"generator-module": {
		"global": false
	}
}
