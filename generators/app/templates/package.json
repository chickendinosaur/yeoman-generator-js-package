{
	"name": "",
	"version": "0.0.1",
	"main": "lib/index.js",
	"scripts": {
		"benchmark": "npm run benchmark-some-module",
		"benchmark-some-module": "npm run test-some-module && node benchmark/some-module.benchmark.js",
		"build": "npm run clean && cp -rf src/* lib/",
		"build-some-module": "cp -f src/some-module.js lib/some-module.js",
		"clean": "rm -rf lib/*",
		"prepublish": "npm run test",
		"test": "npm run build && tap test/**/*.tap.js --reporter spec",
		"test-some-module": "npm run build-some-module && tap test/some-module.tap.js --reporter spec"
	},
	"author": "",
	"license": "MIT",
	"description": "",
	"keywords": [],
	"private": false,
	"repository": {
		"type": "git",
		"url": "git+https://github.com/repoNamespace/repoName.git"
	},
	"bugs": {
		"url": "https://github.com/repoNamespace/repoName/issues"
	},
	"homepage": "https://github.com/repoNamespace/repoName#readme",
	"generator-module": {
		"global": false
	}
}
