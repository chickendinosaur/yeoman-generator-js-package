{
	"name": "",
	"version": "0.0.1",
	"main": "lib/index.js",
	"scripts": {
		"benchmark": "npm run test && node lib/index.benchmark.js",
		"build": "npm run clean && babel src -d build && babel build -d lib --ignore *.spec.js",
		"clean": "rm -rf lib && rm -rf build*",
		"prepublish": "npm run test",
		"test": "npm run build && jest"
	},
	"author": "",
	"license": "MIT",
	"description": "",
	"keywords": [],
	"private": false,
	"repository": {
		"type": "git",
		"url": "git+https://github.com/repoNamespace/repoName.git"
	},
	"bugs": {
		"url": "https://github.com/repoNamespace/repoName/issues"
	},
	"homepage": "https://github.com/repoNamespace/repoName#readme",
	"generator-module": {
		"global": false
	},
	"jest": {
		"testPathDirs": ["build"],
		"testRegex": "\\.spec\\.js$"
	}
}
